# Release Notes

## v0.1
- Added initialization flow in `initilization.py` that creates `Source`, `Output`, and `programs` folders.
- Initialization now ensures `README.txt` and `Run Program.bat` are created.
- Initialization now downloads the helper programs into the `programs/` folder.
